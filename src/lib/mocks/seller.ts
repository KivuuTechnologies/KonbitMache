/**
 * Legacy mock re-export
 *
 * All mock data now lives in @/features/seller/services/mocks
 * This file re-exports for backward compatibility only
 */
export {
  mockSellerProfile,
  mockProducts,
  mockSellerStats,
  mockActivity,
  mockNotifications,
  CURRENT_SELLER_ID,
  productCategories,
  productUnits,
  haitiDepartments,
} from '@/features/seller/services/mocks';
